name: CI

on:
  pull_request:
    branches: [ master ]

jobs:
  check-types:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Super-Linter
      uses: github/super-linter@v3.3.1

